// Решите аналогичную задачу, только определите в какую треть часа попадает указанное количество минут.

let min = 10;

if (min >= 0 && min <= 20) {
	console.log('1 часть');
}

if (min > 20 && min <= 40) {
	console.log('2 часть');
}

if (min > 40 && min <= 60) {
	console.log('3 часть');
}
